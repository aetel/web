{
  "name": "aetel-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "migrate": "node scripts/migrate.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "cheerio": "^1.0.0",
    "gray-matter": "^4.0.3"
  }
}
